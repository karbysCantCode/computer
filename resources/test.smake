.target basic
.flist main_files
.search_add(main_files, all, ".spasm", "..")
.include_directory(basic, "..")
.add_target(basic, main_files)
.output(basic, "../", "basic.bin")
.format(basic, "bin")
.entry(basic, "main")