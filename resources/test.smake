.label re
.target test
.flist main_files
.ifdef re {
    .search_add(main_files, all, ".spasm","resources/test.spasm")
}
.add_target(test, main_files, "resources/test.spasm")
.entry(test, "main")
.output(test, "/build", "testexe.bin")
.format(test, "bin")
